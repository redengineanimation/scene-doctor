# Changelog

## 0.7.x

- **0.7.5** — finalized the add-on description copy.
- **0.7.4** — clearer tagline and description.
- **0.7.3** — bundled install/uninstall instructions.
- **0.7.2** — panel UI overhaul (full-width health bar, severity sort,
  collapsible groups, divider lines, branded masthead) and the full
  custom icon set (logo, per-check glyphs, button glyphs). State-driven
  row status: failing checks show their glyph, clean checks a dim check
  mark.
- **0.7.1** — icon previews load lazily on first draw, never at startup
  (an add-on must never be able to prevent Blender launching).
- **0.7.0** — panel UI polish pass.

## 0.6.x

- **0.6.x** — Lite/Pro build via module strip; license text, copyright
  headers, and sales terms; tier-labeled panel.

## 0.5.0

- Phase 2 checks: broken drivers, broken modifier targets, loose /
  degenerate geometry, duplicate materials, naming hygiene, visibility
  mismatches, empty containers. 3D-Print and Render profiles.

## 0.4.0

- Profiles (General, Game Export), Fix-All-Safe, CSV report export.

## 0.3.0

- Lite/Pro tier scaffolding. Phase 1 checks: non-manifold, doubled
  vertices, sRGB data textures, missing UV maps.

## 0.2.x

- First public build: unapplied transforms, ngons, flipped normals,
  orphan datablocks, missing files. Health score, dismiss, drill-down.
